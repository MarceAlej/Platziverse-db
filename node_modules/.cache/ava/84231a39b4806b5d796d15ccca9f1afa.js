'use strict';

const test = require('ava');

const proxyquire = require('proxyquire');

const config = {
  logging: function () {}
};
const MetricStub = {
  belongsTo: function () {}
};
let AgentStub = null;
let db = null;
test.beforeEach(async () => {
  AgentStub = {
    hasMany: function () {}
  };
  const setupDatabase = proxyquire('../index.js', {
    '../models/agent': () => AgentStub,
    '../models/metric': () => MetricStub
  });
  db = await setupDatabase(config);
});
test('Agent', t => {
  t.truthy(db.Agent, 'Agent service should exist');
});
//# sourceMappingURL=/Users/marce/Desktop/Platzi/Platziverse-db/node_modules/.cache/ava/84231a39b4806b5d796d15ccca9f1afa.js.map